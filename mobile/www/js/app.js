/* this is crude and stupid but we have all jquery here */

 $(document).ready(function(){
   
 
 // angular.element('.messages-content').mCustomScrollbar();
 //  updateScrollbar($messages);  
  // $('.cities').show();  
});

function updateScrollbar() {
   // var $messages = Jquery('.messages-content');
     //angular.element('.messages-content').mCustomScrollbar("update").mCustomScrollbar('scrollTo', 'bottom', {
      scrollInertia: 10,
      timeout: 0
    });
    
  }





